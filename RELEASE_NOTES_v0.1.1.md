# v0.1.1-testfix

This build repairs repository layout, beginner configuration, dry-run platform behavior, dynamic pick/horizon labels, and OCI script path detection. It adds realistic user-scenario tests and passes 11 automated tests.

Keep `DRY_RUN=true` until real provider and Meta canary tests pass.
